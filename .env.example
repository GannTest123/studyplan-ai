# Copy to .env.local and fill in real values. NEVER commit .env.local.

# OpenAI — server-side only, used by /api/generate
OPENAI_API_KEY=

# Supabase — from Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
